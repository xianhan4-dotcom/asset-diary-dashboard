{
  "name": "asset-diary-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only crypto asset snapshots, Excel ledger, risk dashboard and Telegram summary.",
  "scripts": {
    "snapshot": "node --env-file-if-exists=.env src/run_snapshot.mjs --config config.json",
    "sample": "node --env-file-if-exists=.env src/run_snapshot.mjs --config config.example.json --no-telegram --dry-run",
    "dashboard": "node src/serve_dashboard.mjs",
    "check": "node --check src/run_snapshot.mjs && node --check src/write_workbook.mjs && node --check src/write_dashboard_data.mjs && node --check src/serve_dashboard.mjs && node --check web/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
